# 程序级 ADR(demo2027)

只放**跨黑盒**的架构决策——影响多个黑盒之间、或落在契约层的。

- 黑盒**内部**的决策 → 各自仓的 `docs/adr/`。
- **编排 / 工作流**的决策 → EKStation(L0)。

格式同 EKStation:`背景 / 决策 / Considered Options / Consequences`。文件名 `NNNN-kebab-title.md`,从 `0001` 起。
